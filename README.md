# GraphJS Documentation

## Instructions

1. Install prerequisites using the bundle command
    ```
    bundle install
    ```
2. To serve the site locally, 
    ```
    bundle exec jekyll run
    ```